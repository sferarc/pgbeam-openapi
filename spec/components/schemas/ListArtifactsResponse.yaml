type: object
description: Cursor-paginated artifacts for an organization.
required:
  - artifacts
properties:
  artifacts:
    type: array
    description: Artifacts returned for the current page.
    items:
      $ref: ./Artifact.yaml
  next_page_token:
    type: string
    description: Opaque token for cursor-based pagination.
