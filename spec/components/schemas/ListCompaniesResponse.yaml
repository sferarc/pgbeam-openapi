type: object
description: Cursor-paginated companies for an organization.
required:
  - companies
properties:
  companies:
    type: array
    description: Companies returned for the current page.
    items:
      $ref: ./Company.yaml
  next_page_token:
    type: string
    description: Opaque token for cursor-based pagination.
