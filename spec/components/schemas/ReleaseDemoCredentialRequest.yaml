type: object
description: >-
  Release of a demo credential by its holder. The MCP token proves the caller is
  the one that was issued the session.
required:
  - mcp_token
properties:
  mcp_token:
    type: string
    description: The mcp_token returned when the credential was issued.
