type: object
description: >
  A set of project-scoped operations to apply in one request, together with the
  failure semantics the caller wants. Every operation is authorized against the
  caller's scope before any of them runs, and a batch carrying an operation the
  caller's role does not permit is refused whole with 403 rather than
  half-applied. Authorization here is a property of the caller's role rather
  than a per-item runtime condition, so reporting it per item would make "you
  may not do this" look like an ordinary item failure and would invite a caller
  to send a batch of things it may not do to see which stick.
required:
  - mode
  - operations
properties:
  mode:
    type: string
    enum:
      - atomic
      - independent
    description: >
      How the batch treats a failing operation. `atomic` applies all of them or
      none: the first runtime failure rolls the whole batch back and the
      response is 409, so the status code alone never says a batch applied when
      it did not. `independent` applies each operation on its own, so one
      failure stops nothing else and the response is 200 with the outcome of
      each item. There is no default. An operator revoking fifty credentials
      during an incident wants all fifty gone, an operator importing fifty
      column annotations wants the forty-seven that parsed, and picking one of
      those for a caller who did not say would silently do the wrong thing to
      the other.
  operations:
    type: array
    minItems: 1
    maxItems: 100
    description: >
      The operations to apply, in order. At most 100 per request; a longer list
      is rejected with 400 and nothing runs. Each entry must carry a distinct
      `id`, and no two entries may target the same resource.
    items:
      $ref: ./BatchOperation.yaml
