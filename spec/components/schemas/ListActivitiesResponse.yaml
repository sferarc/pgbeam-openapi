type: object
description: Most-recent-first activities for an organization.
required:
  - activities
properties:
  activities:
    type: array
    description: Activities, newest first.
    items:
      $ref: ./Activity.yaml
