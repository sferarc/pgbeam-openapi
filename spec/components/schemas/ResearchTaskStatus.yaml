type: string
description: >
  Lifecycle state of a research task. awaiting_approval means the task is an
  irreversible action held for a human approval before it runs.
enum:
  - pending
  - leased
  - done
  - failed
  - canceled
  - awaiting_approval
