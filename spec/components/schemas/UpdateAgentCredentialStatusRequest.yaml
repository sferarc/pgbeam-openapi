type: object
description: Request body for enabling or disabling an agent credential.
required:
  - status
properties:
  status:
    type: string
    enum:
      - active
      - disabled
    description: >-
      Set active to re-enable or disabled to kill-switch. Use DELETE to revoke
      permanently.
