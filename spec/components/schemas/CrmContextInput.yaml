type: object
description: Request body for updating the org CRM context.
properties:
  positioning:
    type: string
    maxLength: 10000
    description: How the org positions itself.
  icp:
    type: string
    maxLength: 10000
    description: Ideal customer profile.
  tone:
    type: string
    maxLength: 10000
    description: Preferred voice and tone.
  notes:
    type: string
    maxLength: 20000
    description: Any other durable facts.
