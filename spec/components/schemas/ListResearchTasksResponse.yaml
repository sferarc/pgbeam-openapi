type: object
description: Cursor-paginated research tasks for an organization.
required:
  - tasks
properties:
  tasks:
    type: array
    description: Research tasks returned for the current page.
    items:
      $ref: ./ResearchTask.yaml
  next_page_token:
    type: string
    description: Opaque token for cursor-based pagination.
