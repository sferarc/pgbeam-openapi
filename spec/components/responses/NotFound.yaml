description: >-
  The resource does not exist, or the caller is not entitled to know that it
  does. `code` is `NOT_FOUND`.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/Error.yaml
