description: >-
  The request was rejected. `code` is `INVALID_INPUT`, and `errors` names the
  offending fields when the failure was a validation one.
content:
  application/problem+json:
    schema:
      $ref: ../schemas/Error.yaml
